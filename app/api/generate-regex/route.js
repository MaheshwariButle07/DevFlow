const fallback = { regexString: '/^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/', explanation: 'Matches a complete string with text before an @, a domain name, and a dot-separated top-level domain. Whitespace is rejected.' }
export async function POST(request) { const body = await request.json().catch(() => ({})); if (!body.prompt || typeof body.prompt !== 'string' || body.prompt.length > 1000) return Response.json({ error: 'Add a pattern brief under 1,000 characters.' }, { status: 400 }); if (!process.env.GEMINI_API_KEY) return Response.json(fallback, { fallback: true }); const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${process.env.GEMINI_MODEL || 'gemini-3.6-flash'}:generateContent?key=${process.env.GEMINI_API_KEY}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ systemInstruction: { parts: [{ text: 'Return JSON with exactly regexString and explanation. regexString must be a JavaScript-compatible regex literal including flags.' }] }, contents: [{ role: 'user', parts: [{ text: body.prompt }] }], generationConfig: { temperature: 0.2, responseMimeType: 'application/json' } }) }); if (!response.ok) { const errBody = await response.text(); return Response.json({ error: `The model could not generate a regex right now. Gemini error: ${errBody}` }, { status: 502 }); } const result = await response.json(); try { const parsed = JSON.parse(result.candidates[0].content.parts[0].text); if (!parsed.regexString || !parsed.explanation) throw new Error(); return Response.json(parsed) } catch { return Response.json({ error: 'The model returned an invalid regex response.' }, { status: 502 }) } }
